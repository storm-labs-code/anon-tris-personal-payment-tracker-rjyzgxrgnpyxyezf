'use server'

/**
 * CODE INSIGHT
 * This action file is reserved for future server actions that the Tags page may call directly.
 * Current page uses RESTful /api endpoints and offline localForage; no server action is required yet.
 */

export async function noop() {
  return true
}
