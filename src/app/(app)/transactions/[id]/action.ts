'use server'

/**
 * CODE INSIGHT
 * Server actions placeholder for this route. Currently no server-side mutations are needed for the detail page.
 * This file exists to support future enhancements (e.g., server-driven helpers or revalidation hooks).
 */

export async function noop(): Promise<'ok'> {
  return 'ok'
}
