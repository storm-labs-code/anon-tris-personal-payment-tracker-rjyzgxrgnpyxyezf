/**
 * CODE INSIGHT
 * Server actions placeholder for Settings index if future lightweight server interactions are required.
 * Currently unused by the Settings page (navigational only), kept for parity with the action pattern.
 */

'use server'

export async function noopAction() {
  return { ok: true }
}
